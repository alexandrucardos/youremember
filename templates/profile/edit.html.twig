{% extends 'base.html.twig' %}

{% block title %}Editeaza profil{% endblock %}

{% block body %}
    <div class="container py-5 gallery-container position-relative">
        <div class="row">
            {{ include('profile/form.html.twig', {'button_label': 'Update'}) }}
        </div>
        <br>
        <div class="row">
            {{ include('content/form.html.twig', {'button_label': 'Update'}) }}
        </div>
        <br>
        <div class="row">
            {{ include('content/show.html.twig', {images: images}) }}
        </div>
        <br>
        <div class="row">
            <a href="javascript:history.back()" class="btn btn-info btn-lg">Inapoi</a>
        </div>

        {#        <a href="{{ path('app_profile_index') }}">Inapoi</a> #}
    </div>
{% endblock %}
